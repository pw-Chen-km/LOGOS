# LOGOS 2.0 - core runtime dependencies
#
# For editable development prefer:
#   pip install -e .[dev]
# Optional integrations:
#   pip install -e .[neo4j]
#   pip install -e .[docling]
#   pip install -e .[evoscientist]

pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
